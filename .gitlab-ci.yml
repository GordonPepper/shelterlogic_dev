# Test and Deployment file for GitLab CI
Test:
  script:
  - echo 'test success'
  type: test
  tags:
  only:
  - develop
Deploy to Design:
  script:
  - rsync -avxO --chmod=D775,F664 ./* --exclude='app/etc/local.xml' --exclude='app/etc/local.xml.local' --exclude='var/*' --exclude='media/*' --exclude='.git*' --delete design_usr@cl-mgnto-4vap02:/wwwroot/farmbuildings-design.com/http
  - ssh design_usr@cl-mgnto-4vap02 'chmod +x /wwwroot/farmbuildings-design.com/http/*cron.sh'
  type: deploy
  tags:
  only:
  - develop
Deploy to Updates:
  script:
  - rsync -avxO --chmod=D775,F664 ./* --exclude='app/etc/local.xml' --exclude='app/etc/local.xml.local' --exclude='var/*' --exclude='media/*' --exclude='.git*' --delete design_usr@cl-mgnto-4vap02:/wwwroot/farmbuildings-updates.idevdesign.net/http
  - ssh design_usr@cl-mgnto-4vap02 'chmod +x /wwwroot/farmbuildings-updates.idevdesign.net/http/*cron.sh'
  type: deploy
  tags:
  only:
  - farmbuildings-dev
